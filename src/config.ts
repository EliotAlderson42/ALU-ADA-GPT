/**
 * URL de base du backend.
 * Si tu changes le port (ex. uvicorn ... --port 8000),
 * modifie uniquement ici et dans vite.config.ts (proxy target).
 */
export const API_BASE = "http://127.0.0.1:8012";
